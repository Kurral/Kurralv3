# Core dependencies
pydantic>=2.5.0
click>=8.1.7
rich>=13.7.0
python-dotenv>=1.0.0

# Optional: Cloud storage (install as needed for R2 support)
# boto3>=1.34.0

# Optional: PostgreSQL/Supabase (install as needed for metadata storage)
# sqlalchemy>=2.0.0
# psycopg2-binary>=2.9.0

# Optional: LLM clients (install as needed)
# openai>=1.0.0
# anthropic>=0.18.0

